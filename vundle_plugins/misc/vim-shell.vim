" Improved integration between Vim and its environment (fullscreen, open URL,
" background command execution)

if exists('g:vundle_installing_plugins')
	Plugin 'xolox/vim-shell'
	finish
endif

let g:shell_mappings_enabled = 0
